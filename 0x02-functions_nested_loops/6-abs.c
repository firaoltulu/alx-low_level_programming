#include <stdlib.h>
#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: Is an int that is passed to this function from
 * the caller to be computes the absolute value of an integer.
 *
 * Return: the absolute value of int
 */

int _abs(int n)
{
	int four = abs(n);

	return (four);
}

