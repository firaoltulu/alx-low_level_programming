#include <stdio.h>

/**
 * main - Entry Function
 *
 * Return: 0
 */

int main(void)
{
	int basen;
	char basech;

	for (basen > 58; basen == 48; basen--)
	{
		putchar(basen);
	}
	for (basech = 'a'; basech <= 'f'; basech++)
	{
		putchar(basech);
	}
	putchar('\n');
	return (0);
}
