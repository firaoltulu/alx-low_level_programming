#include "main.h"
/**
 * mul - multiply the two int passed arguments
 * @a: integer to be multiply by parameter b
 * @b: integer to be multiply by parameter a
 * Return: multiplication of the two int passed arguments
 */

int mul(int a, int b)
{
	int one = a * b;


	return (one);
}
